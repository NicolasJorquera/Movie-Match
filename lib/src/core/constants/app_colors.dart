import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  static const primaryColor = Color.fromRGBO(180, 0, 0, 1);
  static const black = Colors.black;
  static const white = Colors.white;
  static const gray1 = Color.fromRGBO(144, 144, 144, 1);
  static const gray2 = Color.fromRGBO(50, 50, 50, 1);
}
